import 'package:flutter/material.dart';

class FitchMeasure extends StatelessWidget {
  const FitchMeasure({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('FitchMeasure'),
      ),
      body: Center(
        child: Text('FitchMeasure'),
      ),
    );
  }
}
